export enum Directions {
  RTL = "rtl",
  LTR = "ltr",
}

export enum Languages {
  ENGLISH = "en",
  ARABIC = "ar",
}

export enum Routes {
  ROOT = "/",
  QUIZ_HISTORY = "/quize-history",
  SELECT_QIUZ = "/select-quize",
  AUTH = "/auth",
  ADMIN = "/admin",
}

export enum Pages {
  LOGIN = "signin",
  REGISTER = "signup",
  FORGOT_PASSWORD = "forget-password",
  VERIFY_PASSWORD = "verify-code",
  RESET_PASSWORD = "reset-password",

}
